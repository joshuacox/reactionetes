apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-reaction-api-base-config
data:
  reaction.mongo_url: mongodb://{{ .Values.mongodbName }}-mongodb-replicaset/{{ .Values.reactiondbName }}?replicaSet={{ .Values.mongodbReplicaSet }}
