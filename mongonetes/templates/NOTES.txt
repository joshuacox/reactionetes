MongoDB can be accessed via port 27017 on the following DNS name from within your cluster:
{{ template "mongostateful.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local

To connect to your database run the following command:

   kubectl run {{ template "mongostateful.fullname" . }}-client --rm --tty -i --image mongo:3.4 --command -- mongo --host {{ template "mongostateful.fullname" . }} {{- if .Values.mongodbRootPassword }} -p {{ .Values.mongodbRootPassword }}{{- end -}}
